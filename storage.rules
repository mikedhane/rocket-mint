rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to memecoins folder
    match /memecoins/{allPaths=**} {
      allow read: if true;  // Anyone can read
      allow write: if false; // Only server (Firebase Admin) can write
    }
  }
}
